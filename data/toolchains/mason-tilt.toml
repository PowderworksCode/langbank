id = "mason-tilt"
display-name = "tilt"
kind = "runtime"
languages = ["starlark"]
programs = ["tilt"]
categories = ["runtime", "language-server"]
homepage = "https://tilt.dev/"
repository = "https://github.com/tilt-dev/tilt"

[distribution]
registry = "github"
package = "tilt-dev/tilt"

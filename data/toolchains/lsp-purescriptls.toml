id = "lsp-purescriptls"
display-name = "purescriptls"
kind = "language-server"
languages = ["purescript"]
programs = ["purescript-language-server"]
root-markers = ["bower.json", "flake.nix", "psc-package.json", "shell.nix", "spago.dhall", "spago.yaml"]
categories = ["language-server"]

[distribution]
registry = "npm"
package = "purescript-language-server"

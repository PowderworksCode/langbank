id = "lsp-tsp-server"
display-name = "tsp_server"
kind = "language-server"
languages = ["typespec"]
programs = ["tsp-server"]
root-markers = ["tspconfig.yaml", ".git"]
categories = ["language-server"]
homepage = "https://github.com/microsoft/typespec"

[distribution]
registry = "npm"
package = "%40typespec/compiler"

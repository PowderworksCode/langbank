id = "lsp-just"
display-name = "just"
kind = "language-server"
languages = ["just"]
programs = ["just-lsp"]
root-markers = [".git"]
homepage = "https://github.com/terror/just-lsp/releases"
repository = "https://github.com/terror/just-lsp"

[distribution]
registry = "github"
package = "terror/just-lsp"

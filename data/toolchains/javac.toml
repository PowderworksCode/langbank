id = "javac"
display-name = "javac"
kind = "compiler"
languages = ["java"]
programs = ["javac"]
homepage = "https://openjdk.org"
repository = "https://github.com/openjdk/jdk"

[version]
arguments = ["-version"]
stream = "stdout"
pattern = '^javac (\d+(?:\.\d+)*)'

id = "lsp-gitlab-ci-ls"
display-name = "gitlab_ci_ls"
kind = "language-server"
languages = ["yaml"]
programs = ["gitlab-ci-ls"]
categories = ["language-server"]

[distribution]
registry = "cargo"
package = "gitlab-ci-ls"

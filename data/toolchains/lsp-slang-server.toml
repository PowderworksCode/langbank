id = "lsp-slang-server"
display-name = "slang_server"
kind = "language-server"
languages = ["systemverilog", "verilog"]
programs = ["slang-server"]
root-markers = [".git", ".slang"]
repository = "https://github.com/hudson-trading/slang-server"

[distribution]
registry = "github"
package = "hudson-trading/slang-server"

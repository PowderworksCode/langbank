id = "go"
display-name = "Go"
kind = "compiler"
languages = ["go"]
programs = ["go"]
homepage = "https://go.dev"
repository = "https://github.com/golang/go"

[version]
arguments = ["version"]
stream = "stdout"
pattern = '^go version go(\d+(?:\.\d+)*)'

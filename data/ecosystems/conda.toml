id = "conda"
registry = "conda"
display-name = "conda"
roles = ["package-manager"]
implied-languages = ["python"]
manifest = "environment.yml"
alternate-manifests = ["environment.yaml"]
manifest-selection = "default"
homepage = "https://docs.conda.io"
repository = "https://github.com/conda/conda"

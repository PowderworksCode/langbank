id = "rust"
display-name = "Rust"
role = "programming"
shebangs = ["rust-script"]
extensions = ["rs", "rs.in"]
primary-extensions = ["rs"]
facets = ["structured-code"]
comments = "rust"

[conventions.test-layout]
source-roots = ["src"]
test-root = "tests"
test-suffixes = ["", ".test", ".spec"]

[[conventions.inline-test]]
starts-with = ["#[cfg(test)]"]
indicator = "#[cfg(test)]"

[[conventions.inline-test]]
starts-with = ["#[test]"]
indicator = "#[test]"

[[conventions.inline-test]]
starts-with = ["mod tests"]
indicator = "mod tests"

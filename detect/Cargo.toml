[package]
name = "langbank-detect"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/PowderworksCode/langbank"
description = "Runs what langbank describes: recognise a file by name, and by content where the name is not enough."

[dependencies]
langbank = { path = ".." }
regex = "1"

[dev-dependencies]
regex = "1"

[lints.rust]
unused_must_use = "deny"
